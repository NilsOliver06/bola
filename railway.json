{
  "build": {
    "buildCommand": "dotnet publish -c Release -r linux-x64 --self-contained true -p:PublishSingleFile=true -o /app"
  },
  "deploy": {
    "startCommand": "/app/LOGIN --urls http://0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
