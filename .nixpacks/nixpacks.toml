[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish -c Release -o /app"]

[start]
cmd = "cd /app && dotnet LOGIN.dll --urls http://0.0.0.0:$PORT"